# conda env create -f environment.yml
name: dl1dh
channels:
    - anaconda
    - conda-forge
    - cta-observatory
dependencies:
    - python>=3.9
    - astropy
    - numpy
    - pip
    - ctapipe==0.19.0
    - traitlets>=5.0
    - pyyaml
    - pandas
    - pip:
        - pydot

