# conda env create -f environment.yml
name: dl1dh
channels:
    - anaconda
    - conda-forge
    - cta-observatory
dependencies:
    - python>=3.8
    - astropy
    - numpy
    - pip
    - pyyaml
    - pandas
    - pip:
        - ctapipe==0.12.0
        - pydot

