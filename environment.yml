# conda env create -f environment.yml
name: dl1dh
channels:
    - anaconda
    - conda-forge
    - cta-observatory
dependencies:
    - python>=3.10
    - astropy
    - setuptools
    - numpy
    - scipy
    - pip
    - ctapipe==0.23.2
    - traitlets
    - pyyaml
    - pandas
    - pip:
        - pydot
