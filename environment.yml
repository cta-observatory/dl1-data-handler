# conda env create -f environment.yml
name: dl1dh
channels:
    - anaconda
    - conda-forge
    - cta-observatory
dependencies:
    - python>=3.8
    - astropy<5,>=4.0.5
    - numpy
    - pip
    - ctapipe==0.12.0
    - traitlets>=5.0
    - pyyaml
    - pandas
    - pip:
        - pydot

