# conda env create -f environment.yml
name: dl1dh
channels:
    - conda-forge
dependencies:
    - python>=3.10
    - astropy
    - setuptools
    - numpy
    - scipy
    - pip
    - ctapipe==0.23.2
    - traitlets
    - pyyaml
    - pip:
        - pydot
